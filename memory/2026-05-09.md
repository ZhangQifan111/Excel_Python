# 2026-05-09

## 事件
### 上午
- 奥特曼首次通过微信渠道打招呼，确认后续用 openclaw-weixin 通道沟通
  - chat_id: o9cq80-SweyJhsPdNEG5APmpgO6U@im.wechat
- 设定规则：任务完成后必须微信通知奥特曼，他可能在忙别的事
  - **微信通知要简短**：只说干完了、东西在哪、下一步干嘛，细节留在 webchat
  - **第一时间通知**：子代理完成 → 我 review → 立刻微信通知，不等来问
- 创建定时任务：每天8:30发"我准备好了~"（派大星语录）
- 创建定时任务：每天18:00发"我下班了蟹老板~"
- 主会话模型从 MiMo-V2.5-Pro 切换到 MiMo-V2-Omni（能看图）
- 验证了多会话架构：Omni(项目经理/调度员) + Pro(程序员/子代理)
  - 成功测试：spawn Pro子代理分析项目目录，结果自动回传，微信通知
  - 闭环耗时26s，流程：微信→Omni→spawn Pro→回传→微信通知
  - 核心原则：主会话必须用 tool calling 能力强的模型
- 发现 MiniMax 的 Anthropic API 端点不传图片，配置里加 input:image 无效，已恢复
- 结论：只有 MiMo-V2-Omni 能看图

### 下午 - OTA 代码优化项目
- 分析了两个OTA工程：一体机（共享仓库）和欧三分体机（客制化）
  - 路径: `/Daily Work/2026.5.9 NewOta/aio/Debug_V2/` 和 `/Daily Work/2026.5.9 NewOta/eu3-split/gen2_pcs_arm_renesas_split/`
  - 关键文件: Ota_Config.c, Ota_Rte.c, Ota_Mast.c
  - 分体机 Ota_Mast.c 多317行（THE_ST/SHE_ST逻辑）
  - Ota_Rte.c 两个版本完全一致
- 子代理出了一份完整的OTA代码优化分析报告（MiMo-V2-Pro，3分27秒）
- **第一步已完成**: 抽取 Ota_Common.c/h（通用CRC计算函数 Ota_CommonCalcCrc）
  - 子代理写的代码，我 review 后打包给奥特曼编译验证
  - 初始版本漏了 `#include "bsp_usart.h"`，导致 UART_WIFI_MAX_RX_LEN 未定义，已修复
  - 奥特曼编译验证通过 ✅
- **第二步进行中**: 合并6个BMS/DCDC升级检查函数为1个通用函数
  - 子代理已完成代码改造：新增 Ota_CheckPackUpgrade + 4个版本获取函数
  - 6个旧函数已注释掉，调用处已改为新函数
  - 待奥特曼编译验证
- **架构方向讨论**: 奥特曼同意混合架构方案
  - 通用逻辑抽 Ota_Common（无宏分支）
  - 产品差异用函数指针表隔离（替代 #if defined 宏分支）
  - 只在一处决定用哪个产品配置（ProductConfig.h）
  - 后续第三步：建产品配置表（最大的改动）

## 工作原则更新
- **微信通知要简短**：只说干完了、东西在哪、下一步干嘛，细节留在 webchat
- **第一时间通知**：子代理完成 → 我 review → 立刻微信通知，不等来问
- **打包只打修改文件**：不要每次都打整个工程，只打包改动的文件
- 子代理 spawn 时可以指定 thinking: "high"（写代码用 high，读代码用 medium）
- 子代理 provider 名字要用 config 里定义的（如 `minimax` 而不是 `minimax-cn`）
- auth-profiles.json 在当前版本不生效，API key 直接写 openclaw.json 的 providers 里
- 分工原则：分析/写代码（给定规格）→ 子代理，架构设计/改状态机 → 我自己
- 任务完成通知要同时走微信（message工具）和当前会话

## 技术经验
- MiniMax-M2.7 子代理可用，provider 写 `minimax`，子代理模型列表里写 `minimax/MiniMax-M2.7`
- 子代理 thinking 可选：off/min/medium/high
- 子代理 spawn 支持 label 参数给会话起名，方便追踪
- `OTACRC_FRAME_SIZE` 在 Ota_Common.c 里又 define 了一遍（值相同=512），后续建议统一引用 Ota_Rte.h 里的定义
- Ota_Common.c 用了 `extern uint8_t databuff[]`，需要确保两个文件在同一编译单元
