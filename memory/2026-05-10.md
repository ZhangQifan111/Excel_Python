# 2026-05-10

## 事件
### 上午 - OTA代码优化收尾
- 发现代码在 `/Daily Work/` 目录下（之前搜错路径了，在 /root/ 下没找到）
- 第三步产品配置表已在另一个会话（"弥留之巴尔坦"）完成：
  - ProductConfig/ 下有三个配置文件：default_config.h, the_st_config.h, she_st_config.h
  - ProductConfig.h 作为统一入口，根据编译宏自动选择配置
- **第四步完成：清理重复定义**（ota_第四步_清理重复定义.7z）
  1. Ota_Rte.h — 删除 OTA_USE_AES 宏定义（#if defined 那坨），改为注释说明已移到 ProductConfig
  2. Ota_Common.c — 删除重复的 OTACRC_FRAME_SIZE 定义，改为引用 Ota_Rte.h 里的
  3. the_st_config.h — 删除重复的 GetBms2PackVer/GetDcdc2PackVer 函数，保留 Ota_Common.c 版本

### 模型问题
- 发现当前模型被切成 mimo-v2.5-pro（纯文本），MiMo-V2-Omni 才支持图片
- 手动切换：session_status(model="xiaomicoding/mimo-v2-omni")
- 后续考虑把默认模型改回 mimo-v2-omni，避免每次手动切

## 技术经验
- Ota_Rte.h 原来有 CRLF 行尾，转成了 LF（嵌入式项目要注意行尾兼容）
- ProductConfig.h 目前是空壳（只有 include guard 和 if-else 选择），不引入任何依赖
- en_MCU 枚举在 Ota_Rte.h 和各 ProductConfig 文件里各定义一份（值不同），暂时不合并避免循环依赖
